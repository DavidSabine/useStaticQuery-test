import React from 'react'

const NoProblem = () => (
	<>
		<h1>no-problem.mjs</h1>
		<p>This component does not useStaticQuery.</p>
		<p>It works if the file extension is .js or .mjs</p>
	</>
	)

export default NoProblem
